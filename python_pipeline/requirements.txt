# Python packages for the standalone Klrk1 analysis pipeline.
# Install with:  pip install -r requirements.txt
# (tested with Python 3.12)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pysam>=0.22       # only needed for the junction-counting step (06)
